config:
  target: 'http://localhost:3004'
  phases:
    - duration: 10
      arrivalRate: 1000
scenarios:
  - name: 'test load'
    flow:
      - get:
          url: '/9456781'
# duration is the number of seconds
# arrivalRate is the number of requests per second
